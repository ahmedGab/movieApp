import React from 'react';

const searched = (props) => {
    return (
        <div>
         <div><h6>{props.s.nom.toUpperCase()}</h6><img src={props.s.img} /></div>
         </div>

         )}  



export default searched;